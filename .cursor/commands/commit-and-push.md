Look at the git diff from last commit and then come up with a commit message. Each message should have a title like a summary sentence, but dont explicitly say the word "title" anywhere. Then have short bullet points for major stuff. No need to mention file names or anything. If changes are minor just say, "minor"

Then do git commit -m "THE COMMIT MESSAGE YOU CAME UP WITH". 

After that do git push.
